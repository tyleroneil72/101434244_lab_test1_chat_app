{
  "name": "chat-room",
  "version": "1.0.0",
  "description": "Chat Room App",
  "main": "server/dist/index.js",
  "scripts": {
    "build": "cd client && npm i && npm run build && cd ../server && npm i && npm run build",
    "start": "node server/dist/src/index.js",
    "format": "cd client && npm run format && cd ../server && npm run format",
    "lint": "cd client && npm run lint:prettier && cd ../server && npm run lint:prettier"
  },
  "author": "Tyler O'Neil",
  "license": "MIT"
}
